# CLAUDE.md

## 项目说明

- 本项目是本地运行的个人资产/FIRE 追踪应用，基于 Vite + React + TypeScript。
- 用户的数据模型是“某时间点的全量资产快照”，不是收入支出流水。

## 协作约定

- 默认使用中文沟通。
- 修改代码前先理解现有结构与需求。
- 优先保持实现简单、清晰、可维护。
- 完成前运行 `npm test && npm run build`。
- 涉及 UI/图表改动必须启动浏览器实际检查，不能只依赖测试或构建。
- 本项目只应保留一个 dev server，默认使用 `http://127.0.0.1:5173/`，不要重复启动多个。

## FIRE 功能约定

- FIRE 功能是独立 Tab，放在“复盘报告”右侧，不要把 FIRE 逻辑塞进复盘报告页。
- FIRE 页面保留：月支出、安全提取率、应急备用金月数、预期年化收益率。
- FIRE 页面不要求填写：未来每月主动净投入、收入中断/停投月数。
- 历史速度估算基于资产快照变化，包含最近一次更新、近一年、历史以来；它不受预期年化收益率影响。
- “按预期年化收益率”才受预期年化收益率影响。
